function multiplicacao (a, b) {
    return a * b
}
let x = parseInt(prompt('Digite um número:'));
let y = parseInt(prompt('Digite um número:'));

alert ('A multiplicação resultou em ' + multiplicacao(x, y))
